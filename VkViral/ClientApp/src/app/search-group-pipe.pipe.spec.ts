import { SearchGroupPipePipe } from './search-group-pipe.pipe';

describe('SearchGroupPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchGroupPipePipe();
    expect(pipe).toBeTruthy();
  });
});
